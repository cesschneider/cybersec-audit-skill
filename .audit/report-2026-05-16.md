# Cybersecurity Audit Report

## Executive Summary

| Metric | Value |
|---|---|
| Project | cybersec-audit-skill / vulnerable-demo |
| Date | 2026-05-16 |
| Files Scanned | 4 |
| Total Vulnerabilities | 33 |
| Critical | 14 |
| High | 10 |
| Medium | 8 |
| Low | 1 |

**Risk Score: 100/100** — *Extremely High Risk. These apps should never be deployed as-is.*

## Tools Used

| Layer | Tool | Result |
|---|---|---|
| Layer 1: Deterministic | grep patterns | 27 findings |
| Layer 2: SAST | semgrep 1.163.0 | semgrep found 24 issues (12 ERROR + 12 WARNING) on Flask app |
| Layer 2: SAST | bandit 1.9.4 | bandit found 11 issues (3 High, 8 Medium) on Flask app |
| Layer 3: LLM Context | AI analysis | 7 contextual findings |

---

## Critical Findings (14)

## CRITICAL Findings (14)

### [CRIT-001] Sql Injection
- **Severity:** CRITICAL
- **Category:** sql_injection / CWE-89
- **File:** `app.py:44`
- **Description:** SQL query constructed via f-string with user input from request.args. Attacker can inject arbitrary SQL via ?id=1 OR 1=1.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 44):
  query = f"SELECT * FROM users WHERE id = {user_id}"

  // FIXED:
  db.execute('SELECT * FROM users WHERE id = ?', (user_id,))
  ```
- **Detection:** grep + semgrep + bandit
- **Auto-fix Ready:** Yes

### [CRIT-002] Sql Injection
- **Severity:** CRITICAL
- **Category:** sql_injection / CWE-89
- **File:** `app.py:53`
- **Description:** SQL query constructed via string concatenation with user input. Allows full SQL injection.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 53):
  query = "SELECT * FROM users WHERE username LIKE '%" + name + "%'"

  // FIXED:
  db.execute('SELECT * FROM users WHERE username LIKE ?', (f'%{name}%',))
  ```
- **Detection:** grep + semgrep + bandit
- **Auto-fix Ready:** Yes

### [CRIT-003] Sql Injection
- **Severity:** CRITICAL
- **Category:** sql_injection / CWE-89
- **File:** `app.py:62`
- **Description:** SQL query built using .format() with user-controlled email parameter.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 62):
  query = "SELECT * FROM users WHERE email = '{}'".format(email)

  // FIXED:
  db.execute('SELECT * FROM users WHERE email = ?', (email,))
  ```
- **Detection:** grep + semgrep + bandit
- **Auto-fix Ready:** Yes

### [CRIT-004] Command Injection
- **Severity:** CRITICAL
- **Category:** command_injection / CWE-78
- **File:** `app.py:71`
- **Description:** User input directly executed as shell command via os.system(). Attacker can run arbitrary commands.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 71):
  os.system(f"ping -c 3 {host}")

  // FIXED:
  subprocess.run(["ping", "-c", "3", host], capture_output=True, text=True, check=True)
  ```
- **Detection:** grep + semgrep + bandit
- **Auto-fix Ready:** Yes

### [CRIT-005] Command Injection
- **Severity:** CRITICAL
- **Category:** command_injection / CWE-78
- **File:** `app.py:79`
- **Description:** subprocess with shell=True and user input. Allows command injection via ;, |, or &&.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 79):
  subprocess.run(f"nslookup {target}", shell=True)

  // FIXED:
  subprocess.run(["nslookup", target], capture_output=True, text=True)
  ```
- **Detection:** grep + semgrep + bandit
- **Auto-fix Ready:** Yes

### [CRIT-006] Sql Injection
- **Severity:** CRITICAL
- **Category:** sql_injection / CWE-89
- **File:** `app.py:136`
- **Description:** Authentication bypass via SQL injection. Attacker can login as any user with username=admin'--.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 136):
  query = f"SELECT * FROM users WHERE username = '{username}' AND password = '{password}'"

  // FIXED:
  db.execute('SELECT * FROM users WHERE username = ?', (username,))
# Then compare hashed password
  ```
- **Detection:** grep + semgrep + bandit
- **Auto-fix Ready:** Yes

### [CRIT-007] Hardcoded Secret
- **Severity:** CRITICAL
- **Category:** hardcoded_secret / CWE-798
- **File:** `config.py:5`
- **Description:** Database credentials with username and password hardcoded in source code.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 5):
  DATABASE_URL = 'postgresql://root:root_password_123@localhost:5432/myapp'

  // FIXED:
  DATABASE_URL = os.environ.get('DATABASE_URL')
  ```
- **Detection:** grep
- **Auto-fix Ready:** Yes

### [CRIT-008] Sql Injection
- **Severity:** CRITICAL
- **Category:** sql_injection / CWE-89
- **File:** `server.js:44`
- **Description:** SQL injection via template literal with user input from request query parameter.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 44):
  db.query(`SELECT * FROM users WHERE username LIKE '%${search}%'`)

  // FIXED:
  const [rows] = await db.query('SELECT * FROM users WHERE username LIKE ?', [`%${search}%`])
  ```
- **Detection:** grep + semgrep
- **Auto-fix Ready:** Yes

### [CRIT-009] Sql Injection
- **Severity:** CRITICAL
- **Category:** sql_injection / CWE-89
- **File:** `server.js:53`
- **Description:** SQL injection via string concatenation with URL parameter.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 53):
  db.query("SELECT * FROM users WHERE id = " + id)

  // FIXED:
  const [rows] = await db.query('SELECT * FROM users WHERE id = ?', [id])
  ```
- **Detection:** grep + semgrep
- **Auto-fix Ready:** Yes

### [CRIT-010] Command Injection
- **Severity:** CRITICAL
- **Category:** command_injection / CWE-78
- **File:** `server.js:69`
- **Description:** Command injection via child_process.exec with user-controlled target parameter.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 69):
  exec(`ping -c 3 ${target}`)

  // FIXED:
  const { execFile } = require('child_process')
execFile('ping', ['-c', '3', target], callback)
  ```
- **Detection:** grep + semgrep
- **Auto-fix Ready:** Yes

### [CRIT-011] Command Injection
- **Severity:** CRITICAL
- **Category:** command_injection / CWE-95
- **File:** `server.js:79`
- **Description:** eval() executes arbitrary JavaScript from user input. Complete remote code execution.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 79):
  eval(expression)

  // FIXED:
  const math = require('mathjs')
const result = math.evaluate(expression)
  ```
- **Detection:** grep
- **Auto-fix Ready:** Yes

### [CRIT-012] Sql Injection
- **Severity:** CRITICAL
- **Category:** sql_injection / CWE-89
- **File:** `server.js:122`
- **Description:** SQL injection in admin delete endpoint via template literal. Combined with no auth, allows arbitrary data destruction.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 122):
  db.query(`DELETE FROM users WHERE id = ${id}`)

  // FIXED:
  await db.query('DELETE FROM users WHERE id = ?', [id])
  ```
- **Detection:** grep
- **Auto-fix Ready:** Yes

### [CRIT-013] Sql Injection
- **Severity:** CRITICAL
- **Category:** sql_injection / CWE-89
- **File:** `server.js:61`
- **Description:** SQL injection in user creation endpoint — attacker can create admin accounts or inject malicious data.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 61):
  db.query(`INSERT INTO users (username, email) VALUES ('${username}', '${email}')`)

  // FIXED:
  await db.query('INSERT INTO users (username, email) VALUES (?, ?)', [username, email])
  ```
- **Detection:** grep
- **Auto-fix Ready:** Yes

### [CRIT-014] Hardcoded Secret
- **Severity:** CRITICAL
- **Category:** hardcoded_secret / CWE-798
- **File:** `config.py:6`
- **Description:** API secret key hardcoded in configuration file.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 6):
  API_SECRET = 'sk-proj-5f7a9b2c3d4e...'

  // FIXED:
  API_SECRET = os.environ.get('API_SECRET')
  ```
- **Detection:** grep
- **Auto-fix Ready:** Yes

## HIGH Findings (10)

### [HIGH-001] Xss Reflected
- **Severity:** HIGH
- **Category:** xss_reflected / CWE-79
- **File:** `app.py:87`
- **Description:** Reflected XSS — user input from request.args rendered directly in HTML without escaping.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 87):
  return f'<h1>Hello, {name}!</h1>'

  // FIXED:
  return jsonify({'message': f'Hello, {name}!'})
  ```
- **Detection:** grep + semgrep
- **Auto-fix Ready:** Yes

### [HIGH-002] Xss Reflected
- **Severity:** HIGH
- **Category:** xss_reflected / CWE-79
- **File:** `app.py:93`
- **Description:** Reflected XSS via error message parameter.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 93):
  return f'<div class="error">Error: {msg}</div>'

  // FIXED:
  return jsonify({'error': msg})
  ```
- **Detection:** grep + semgrep
- **Auto-fix Ready:** Yes

### [HIGH-003] Path Traversal
- **Severity:** HIGH
- **Category:** path_traversal / CWE-22
- **File:** `app.py:100`
- **Description:** User controls filename without validation. Allows reading any file via ../../path traversal.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 100):
  return send_from_directory(os.path.dirname(__file__), filename)

  // FIXED:
  real_path = os.path.realpath(os.path.join(base_dir, filename))
if not real_path.startswith(base_dir): abort(403)
  ```
- **Detection:** grep
- **Auto-fix Ready:** Yes

### [HIGH-004] Broken Auth
- **Severity:** HIGH
- **Category:** broken_auth / CWE-284
- **File:** `app.py:103`
- **Description:** Admin endpoint with no authentication or authorization check. Any user can delete other users.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 103):
  @app.route('/admin/delete-user/<int:user_id>', methods=['DELETE'])

  // FIXED:
  @login_required
@role_required('admin')
@app.route('/admin/delete-user/<int:user_id>', methods=['DELETE'])
  ```
- **Detection:** llm
- **Auto-fix Ready:** Yes

### [HIGH-005] Information Disclosure
- **Severity:** HIGH
- **Category:** information_disclosure / CWE-209
- **File:** `app.py:120`
- **Description:** Full Python traceback and exception details exposed to client on error.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 120):
  return jsonify({'error': str(e), 'traceback': traceback.format_exc()})

  // FIXED:
  logger.exception(e)
return jsonify({'error': 'Internal server error'}), 500
  ```
- **Detection:** llm
- **Auto-fix Ready:** Yes

### [HIGH-006] Weak Crypto
- **Severity:** HIGH
- **Category:** weak_crypto / CWE-327
- **File:** `utils.py:12`
- **Description:** MD5 used for password hashing. MD5 is cryptographically broken and can be brute-forced or reversed.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 12):
  hashlib.md5(password.encode()).hexdigest()

  // FIXED:
  import bcrypt
return bcrypt.hashpw(password.encode(), bcrypt.gensalt()).decode()
  ```
- **Detection:** grep + semgrep + bandit
- **Auto-fix Ready:** Yes

### [HIGH-007] Path Traversal
- **Severity:** HIGH
- **Category:** path_traversal / CWE-22
- **File:** `server.js:91`
- **Description:** Path traversal via user-controlled filename. Can read any file on disk using ../../.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 91):
  fs.readFile(path.join(__dirname, 'uploads', filename))

  // FIXED:
  const safePath = path.resolve(__dirname, 'uploads', filename)
if (!safePath.startsWith(path.resolve(__dirname, 'uploads'))) return res.status(403).send('Forbidden')
  ```
- **Detection:** grep + semgrep
- **Auto-fix Ready:** Yes

### [HIGH-008] Xss Reflected
- **Severity:** HIGH
- **Category:** xss_reflected / CWE-79
- **File:** `server.js:102`
- **Description:** Reflected XSS — user input rendered in HTML template literal without escaping.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 102):
  res.send(`<h1>Hello, ${name}!</h1>`)

  // FIXED:
  res.json({ message: `Hello, ${name}!` })
  ```
- **Detection:** grep + semgrep
- **Auto-fix Ready:** Yes

### [HIGH-009] Ssrf
- **Severity:** HIGH
- **Category:** ssrf / CWE-918
- **File:** `server.js:109`
- **Description:** Server-Side Request Forgery — attacker controls URL, can access internal services (localhost, AWS metadata, etc.).
- **Code Snippet:**
  ```python
  // VULNERABLE (line 109):
  https.get(url, ...)

  // FIXED:
  const { URL } = require('url')
const parsed = new URL(url)
if (parsed.hostname === 'localhost' || parsed.hostname.startsWith('169.254.')) return res.status(403)
  ```
- **Detection:** llm
- **Auto-fix Ready:** Yes

### [HIGH-010] Broken Auth
- **Severity:** HIGH
- **Category:** broken_auth / CWE-284
- **File:** `server.js:117`
- **Description:** Admin delete endpoint with no authentication middleware. Any user can delete any other user.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 117):
  app.delete('/api/admin/users/:id', ...)

  // FIXED:
  app.delete('/api/admin/users/:id', authenticate, requireAdmin, async (req, res) => { ... })
  ```
- **Detection:** llm
- **Auto-fix Ready:** Requires manual review

## MEDIUM Findings (8)

### [MED-001] Insecure Config
- **Severity:** MEDIUM
- **Category:** insecure_config / CWE-16
- **File:** `app.py:18`
- **Description:** Debug mode enabled exposes Werkzeug debugger, stack traces, and allows code execution via debugger console.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 18):
  app.config['DEBUG'] = True

  // FIXED:
  app.config['DEBUG'] = os.environ.get('FLASK_DEBUG', 'false').lower() == 'true'
  ```
- **Detection:** grep
- **Auto-fix Ready:** Yes

### [MED-002] Hardcoded Secret
- **Severity:** MEDIUM
- **Category:** hardcoded_secret / CWE-798
- **File:** `config.py:4`
- **Description:** Flask SECRET_KEY hardcoded. Can be used to forge session cookies.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 4):
  SECRET_KEY = 'flask-secret-key-hardcoded-2024'

  // FIXED:
  SECRET_KEY = os.environ.get('SECRET_KEY', secrets.token_hex(32))
  ```
- **Detection:** grep
- **Auto-fix Ready:** Yes

### [MED-003] Insecure Config
- **Severity:** MEDIUM
- **Category:** insecure_config / CWE-352
- **File:** `config.py:14`
- **Description:** Session cookies accessible via JavaScript (HttpOnly=False), enabling XSS-based session theft.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 14):
  SESSION_COOKIE_HTTPONLY = False

  // FIXED:
  SESSION_COOKIE_HTTPONLY = True
SESSION_COOKIE_SECURE = True
  ```
- **Detection:** grep + llm
- **Auto-fix Ready:** Yes

### [MED-004] Insecure Config
- **Severity:** MEDIUM
- **Category:** insecure_config / CWE-942
- **File:** `app.py:145`
- **Description:** CORS allows any origin to make cross-site requests. Combined with session cookies, this enables CSRF attacks.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 145):
  response.headers['Access-Control-Allow-Origin'] = '*'

  // FIXED:
  ALLOWED_ORIGINS = os.environ.get('ALLOWED_ORIGINS', '').split(',')
if origin in ALLOWED_ORIGINS: response.headers['Access-Control-Allow-Origin'] = origin
  ```
- **Detection:** grep
- **Auto-fix Ready:** Yes

### [MED-005] Weak Crypto
- **Severity:** MEDIUM
- **Category:** weak_crypto / CWE-330
- **File:** `utils.py:18`
- **Description:** Using Python's random (non-cryptographic PRNG) for security token generation. Predictable by attacker.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 18):
  random.randint(100000, 999999)

  // FIXED:
  import secrets
return str(secrets.randbelow(900000) + 100000)
  ```
- **Detection:** grep
- **Auto-fix Ready:** Yes

### [MED-006] Idor
- **Severity:** MEDIUM
- **Category:** idor / CWE-639
- **File:** `server.js:131`
- **Description:** Insecure Direct Object Reference — user can access any profile by manipulating userId parameter.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 131):
  db.query(`SELECT * FROM users WHERE id = ${userId}`)

  // FIXED:
  // Check req.user.id matches userId or has admin role
  ```
- **Detection:** llm
- **Auto-fix Ready:** Requires manual review

### [MED-007] Information Disclosure
- **Severity:** MEDIUM
- **Category:** information_disclosure / CWE-209
- **File:** `server.js:160`
- **Description:** Debug endpoint exposes entire process.env, including all secrets, API keys, and internal hostnames.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 160):
  res.json({ env: process.env, ... })

  // FIXED:
  // Remove this endpoint entirely in production
  ```
- **Detection:** llm
- **Auto-fix Ready:** Yes

### [MED-008] Hardcoded Secret
- **Severity:** MEDIUM
- **Category:** hardcoded_secret / CWE-798
- **File:** `server.js:19`
- **Description:** Database password hardcoded in source code.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 19):
  const DB_PASS = 'Pr0duction_Passw0rd!2024'

  // FIXED:
  const DB_PASS = process.env.DB_PASSWORD
  ```
- **Detection:** grep
- **Auto-fix Ready:** Yes

## LOW Findings (1)

### [LOW-001] Insecure Config
- **Severity:** LOW
- **Category:** insecure_config / CWE-200
- **File:** `app.py:151`
- **Description:** Server binds to all interfaces (0.0.0.0), making it accessible from any network interface.
- **Code Snippet:**
  ```python
  // VULNERABLE (line 151):
  app.run(host='0.0.0.0', port=5000)

  // FIXED:
  app.run(host=os.environ.get('HOST', '127.0.0.1'), port=int(os.environ.get('PORT', 5000)))
  ```
- **Detection:** grep + semgrep
- **Auto-fix Ready:** Yes


## Remediation Priority

1. **Immediate (Critical — 14 findings):** Fix all SQL injection and command injection vulnerabilities. Remove hardcoded credentials. These allow complete system compromise.
2. **Short-term (High — 10 findings):** Add output escaping for XSS. Implement authentication on admin routes. Fix path traversal. Address SSRF.
3. **Planned (Medium — 8 findings):** Secure configuration (DEBUG mode, CORS, cookies). Add auth middleware for IDOR protection. Remove debug endpoints.
4. **Backlog (Low — 1 findings):** Bind to localhost instead of 0.0.0.0.

---
*Generated by CloudScream v1.0.0 — Hybrid 3-layer Audit (Deterministic + SAST + LLM)*
